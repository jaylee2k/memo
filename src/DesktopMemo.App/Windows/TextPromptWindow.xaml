<Window x:Class="DesktopMemo.App.Windows.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="입력"
        Width="420"
        Height="170"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Name="MessageText" Grid.Row="0" Margin="0,0,0,8" TextWrapping="Wrap" />
        <TextBox Name="InputText" Grid.Row="1" Height="28" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="확인" Width="84" Margin="0,0,8,0" Click="OkButton_OnClick" />
            <Button Content="취소" Width="84" Click="CancelButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
